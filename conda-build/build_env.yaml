name: buildenv
channels:
  - conda-forge
dependencies:
  - anaconda
  - conda-build<25 # from 25 the output file is a .conda not .tar.bz2
  - anaconda-client
  - conda-verify
  # - anaconda::py-lief>=16  
  # - conda-forge/label/python_rc::_python_rc # Dummy package to force selection of python_rc channel when channel_priority is set to strict
